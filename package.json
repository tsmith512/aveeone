{
  "name": "aveeone",
  "version": "0.1.0",
  "description": "Cloudflare Worker + Container that transcodes MP4 video to AV1 on the fly with ffmpeg",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250607.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
